#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

echo "==== SeeDance restart ===="
bash "$SCRIPT_DIR/stop"
sleep 1
bash "$SCRIPT_DIR/start"